jobs:
  build:
    docker:
      - image: cimg/base:stable
    parallelism: 3 
    steps:
      - checkout
      - run: echo "Hello"
  test:
    docker:
      - image: cimg/base:stable
    parallelism: 3 
    steps:
      - checkout
      - run: echo "Hello"
workflows:
  version: 2
  nend-build:
    jobs:
      - build
      - test
